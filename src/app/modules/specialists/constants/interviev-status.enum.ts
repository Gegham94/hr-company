export enum SpecialistInterviewStatusEnum {
  ACCEPTED = "accepted",
  FAVORITES = "favorites",
  REJECTED = "rejected",
  AWAITS = "awaits",
  SUCCESS = "success",
  FAILED = "failed",
  InProgress = "inProgress",
}
